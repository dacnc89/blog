List articles

<% @articles.each do |article| %>
  <%= article.title %><br>
  <%= article.text %>


<%= link_to 'Show', article %>
<%= link_to 'Edit', edit_article_path(article) %>
<%= link_to 'Delete', article, method: :delete, data: {confirm: 'Do you want to delete this article?'} %>
<% end %>

<div class="clearfix">
</div>

<%= link_to 'New Article', new_article_path %>
