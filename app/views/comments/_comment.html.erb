<h3>
  <%= comment.commenter %>
</h3>

<br>
<%= comment.body %>

<% if user_signed_in? && session[:user_id] == comment.user.id %>
<!-- <%= session[:user_id] %>
 <%= comment.user.id %> -->
<%= link_to 'Edit', edit_article_comment_path(comment.article_id, comment) %>
<%= link_to 'Delete', article_comment_path(comment.article_id, comment), method: :delete, data: {confirm: 'Do you want to delete this comment?'} %>
<% end %>