<h3>
  <%= comment.commenter %>
</h3>

<br>
<%= comment.body %>

<%= link_to 'Edit comment', edit_article_comment_path(comment.article_id, comment) %>
<%= link_to 'Delete comment', article_comment_path(comment.article_id, comment), method: :delete, data: {confirm: 'Do you want to delete this comment?'} %>
